# Copy this file to .env and fill in the values before deploying

# Password to access the web dashboard.
# Leave empty to disable auth (local dev only — never do this on a VPS).
GIVENUM_PASSWORD=your-strong-password-here

# Port the dashboard will listen on (default: 3000)
APP_PORT=3000
